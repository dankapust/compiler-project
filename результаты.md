# Статус выполнения MiniCompiler (Python)

MiniCompiler — учебный мини-компилятор для упрощённого C‑подобного языка.

**О документе:** Ниже зафиксировано выполнение требований **Sprint 1**, **Sprint 2** и **Sprint 3**.

---

## Sprint 1 — Лексический анализатор (Lexer/Scanner)

### Структура / сборка / документация

| Требование | Статус | Доказательство |
| --- | --- | --- |
| **STR-1** | Выполнено | Структура директорий: `src/`, `tests/`, `examples/`, `docs/` |
| **STR-2** | Выполнено | Python packaging: `pyproject.toml`, entry point `compiler` |
| **STR-3** | Выполнено | `README.md` + `docs/language_spec.md` |
| **STR-4** | Выполнено | Модули разделены: `src/lexer/*` и `src/utils/*` |

### Спецификация языка (лексика)

| Требование | Статус | Доказательство |
| --- | --- | --- |
| **LANG-1** | Выполнено | `docs/language_spec.md` (EBNF/regex, UTF‑8) |
| **LANG-2** | Выполнено | Список keywords в `docs/language_spec.md` + `src/lexer/scanner.py` |
| **LANG-3** | Выполнено | Правила identifier + max 255 + тест `tests/lexer/invalid/test_identifier_too_long.*` |
| **LANG-4** | Выполнено | int/float/string/bool |
| **LANG-5** | Выполнено | Операторы и разделители: `src/lexer/token.py`, тест `tests/lexer/valid/test_operators.*` |
| **LANG-6** | Выполнено | Whitespace + `//` + `/*...*/` (с nesting) |

### Реализация лексера

| Требование | Статус | Доказательство |
| --- | --- | --- |
| **LEX-1** | Выполнено | `src/lexer/token.py` (type/lexeme/line/column/literal) |
| **LEX-2** | Выполнено | `src/lexer/scanner.py`: `next_token`, `peek_token`, `is_at_end`, `get_line`, `get_column` |
| **LEX-3** | Выполнено | Keywords/identifiers/literals/operators/delimiters/EOF; maximal munch для `==`, `+=` и т.д. |
| **LEX-4** | Выполнено | CRLF поддержан: `tests/lexer/valid/test_crlf_line_endings.*` |
| **LEX-5** | Выполнено | ERROR tokens + `errors` list + понятные сообщения |
| **LEX-6** | Выполнено | O(n) сканирование, без "тяжёлых" копирований |

### Тесты

| Требование | Статус | Доказательство |
| --- | --- | --- |
| **TEST-1** | Выполнено | valid/invalid cases по категориям в `tests/lexer/**` |
| **TEST-2** | Выполнено | `tests/lexer/valid/` и `tests/lexer/invalid/` |
| **TEST-3** | Выполнено | Формат: `LINE:COLUMN TOKEN_TYPE "LEXEME" [VALUE]` в `.tokens` |
| **TEST-4** | Выполнено | Golden runner: `tests/test_runner/runner.py` |
| **TEST-5** | Выполнено | 21 valid и 11 invalid + CRLF test |

### Stretch Goal — Препроцессор

| Требование | Статус | Доказательство |
| --- | --- | --- |
| **PRE-1** | Выполнено | `src/preprocessor/preprocessor.py`: удаление `//` и `/* */`, сохранение номеров строк |
| **PRE-2** | Выполнено | `#define NAME value`, `#undef`, `#ifdef`, `#ifndef`, `#endif` |
| **PRE-3** | Выполнено | `Preprocessor(source)`, `process()`, `define()`, `undefine()` |
| **PRE-4** | Выполнено | Комментарии в строках сохраняются; незакрытые комментарии — ошибка; рекурсия макросов — ошибка |

---

## Sprint 2 — Парсер и AST

### Как это работает

1. **Исходный код** (`*.src`) подаётся на вход
2. **Препроцессор** удаляет комментарии и раскрывает макросы
3. **Лексер** (Scanner) разбивает текст на токены (Sprint 1)
4. **Парсер** (Parser) строит AST из потока токенов методом рекурсивного спуска
5. **Визуализация** — AST можно вывести в текстовом, DOT (Graphviz) или JSON формате

### Формальная грамматика (GRAM-1 — GRAM-6)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **GRAM-1** | Выполнено | `src/parser/grammar.txt` — полная EBNF грамматика |
| **GRAM-2** | Выполнено | Start symbol `Program`, нетерминалы для всех конструкций, терминалы = TokenType из Sprint 1 |
| **GRAM-3** | Выполнено | 9 уровней приоритета: Primary → Unary → Multiplicative → Additive → Relational → Equality → LogicalAnd → LogicalOr → Assignment |
| **GRAM-4** | Выполнено | Block, VarDecl, Assignment, If/Else, While, For, Return, Call, EmptyStmt (`;`) |
| **GRAM-5** | Выполнено | FunctionDecl (`fn name(params) -> type { ... }`), VarDecl, StructDecl |
| **GRAM-6** | Выполнено | Left-assoc: `+ - * / % && \|\|`; Right-assoc: `= += -= *= /= %=`; Non-assoc: `== != < <= > >=` |

### Парсер (PAR-1 — PAR-5)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **PAR-1** | Выполнено | `Parser(tokens)`, `parse() -> ProgramNode`, ошибки с line/column, интеграция с лексером |
| **PAR-2** | Выполнено | Рекурсивный спуск: `_declaration()`, `_statement()`, `_expression()`, `_assignment()` → `_primary()`. Хелперы: `_match()`, `_consume()`, `_peek()`, `_is_at_end()` |
| **PAR-3** | Выполнено | Ошибки с позицией и ожидаемым токеном. Обработка пропущенных `;`, несовпадающих скобок. Panic-mode recovery (`_synchronize()`) |
| **PAR-4** | Выполнено | 1-token lookahead. Disambiguation: `_is_type_start()` проверяет `type_keyword` или `IDENTIFIER IDENTIFIER`. Dangling else — `else` привязывается к ближайшему `if` |
| **PAR-5** | Выполнено | O(n) для LL(1) грамматики. Память пропорциональна размеру AST |

### AST (AST-1 — AST-5)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **AST-1** | Выполнено | `ASTNode` (base, line/column), `ProgramNode`, Expression/Statement/Declaration подклассы |
| **AST-2** | Выполнено | `LiteralExpr`, `IdentifierExpr`, `BinaryExpr`, `UnaryExpr`, `CallExpr`, `AssignmentExpr` |
| **AST-3** | Выполнено | `BlockStmt`, `ExprStmt`, `IfStmt`, `WhileStmt`, `ForStmt`, `ReturnStmt`, `VarDeclStmt`, `EmptyStmt` |
| **AST-4** | Выполнено | `FunctionDecl`, `StructDecl`, `Param` |
| **AST-5** | Выполнено | `ASTVisitor` base class + `accept(visitor)` на каждом узле. Pretty printer и DOT generator используют visitor |

---

## Sprint 3 — Семантический анализ

### Структура / документация / CLI

| Требование | Статус | Доказательство |
| --- | --- | --- |
| **STR-1** | Выполнено | Полный прогон `python -m tests.test_runner` проходит (lex+parse+semantic). |
| **STR-2** | Выполнено | `src/semantic/{analyzer.py,symbol_table.py,type_system.py,errors.py,output.py}` |
| **STR-3** | Выполнено | `README.md`: команды `compiler check`, `compiler symbols`, опции `--show-types`, `--report` |

### Таблица символов и области видимости (SYM-1 — SYM-5)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **SYM-1** | Выполнено | `SymbolTable`: `enter_scope/exit_scope/insert/lookup/lookup_local` |
| **SYM-2** | Выполнено | Global / function / block / loop + глубина вложенности; поля структур доступны через `x.field` (MemberAccess) |
| **SYM-3** | Выполнено | `SymbolInfo`: kind/type/loc + fn params/ret + struct fields + initialized + stack offset/size/alignment |
| **SYM-4** | Выполнено | `Type`: базовые + struct + function + `types_equal` |
| **SYM-5** | Выполнено (Should) | `type_size_bytes/type_alignment`; offsets для параметров/локальных |

### Семантические проверки (SEM-1 — SEM-6)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **SEM-1** | Выполнено | `SemanticAnalyzer.analyze()`, `get_errors/get_symbol_table/get_decorated_ast` |
| **SEM-2** | Выполнено | Дубликаты, предварительная регистрация `struct` и сигнатур функций (forward refs) |
| **SEM-3** | Выполнено | Инференс типов выражений + правила операторов + совместимость присваивания |
| **SEM-4** | Выполнено | Проверка вызовов функций: count/types + return type |
| **SEM-5** | Выполнено | Необъявленные имена; use-before-init: базовый флаг + flow-sensitive merge для `if/else` и консервативно для `while` |
| **SEM-6** | Выполнено (частично по языку) | Условия `if/while/for` должны быть `bool`; проверка обновления переменной цикла в `for`. `break/continue` отсутствуют в AST языка |

### Ошибки и отчёты (ERR-1 — ERR-4, DEC-1 — DEC-3)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **ERR-1** | Выполнено | Категории: undeclared/duplicate/type mismatch/arg mismatch/return/condition/lvalue/scope |
| **ERR-2** | Выполнено | `SemanticError.format()`: file:line:col + контекст + expected/found + caret `^` |
| **ERR-3** | Выполнено | Анализ продолжается, ошибки копятся в список |
| **ERR-4** | Выполнено | Формат `семантическая ошибка ... --> file:line:col` |
| **DEC-1** | Выполнено (с расширением) | Типы на выражениях + symbol refs + resolved calls + constant folding (литералы + простые унарные/бинарные) |
| **DEC-2** | Выполнено | `compiler check`: отчёт + символы + типы + decorated AST; `compiler symbols --format json` |
| **DEC-3** | Выполнено (Should) | Validation report: counts + symbols-by-scope + summary type hierarchy + layout в dump |

### Тесты семантики (TEST-1 — TEST-5)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **TEST-1** | Выполнено | Юнит-тесты в `tests/semantic/test_symbol_table.py` (scopes, types, calls, flow init) |
| **TEST-2** | Выполнено | `tests/semantic/{valid,invalid}/...` включая `struct_fields/` |
| **TEST-3** | Выполнено | Golden-тесты `.expected` через `python -m tests.test_runner --only semantic` |
| **TEST-4** | Выполнено (Should) | Полный пайплайн используется в golden runner (pp→lex→parse→semantic) |
| **TEST-5** | Частично (Should) | Есть multi-error + caret; расширять можно по качеству сообщений/локаций |

### Визуализация (VIS-1 — VIS-4)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **VIS-1** | Выполнено | `src/parser/pretty.py` — текстовый формат с отступами, скобками для приоритета, типами |
| **VIS-2** | Выполнено | `src/parser/dot.py` — Graphviz DOT с цветовой кодировкой (declarations=голубой, statements=жёлтый, expressions=зелёный) |
| **VIS-3** | Выполнено | `src/parser/codec.py` — JSON encode/decode с round-trip проверкой |
| **VIS-4** | Выполнено | CLI: `compiler parse --ast-format [text\|dot\|json] --output ... --verbose --max-errors N` |

### Тесты (TEST-1 — TEST-5)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **TEST-1** | Выполнено | Тесты на каждое правило: выражения (приоритет, unary, call, assignment), statements (if/else, while, for, blocks), declarations (functions, structs) |
| **TEST-2** | Выполнено | `tests/parser/valid/{expressions,statements,declarations,full_programs}/`, `tests/parser/invalid/syntax_errors/` |
| **TEST-3** | Выполнено | Golden testing: `.expected` файлы с AST + ошибками, автоматическое сравнение с diff |
| **TEST-4** | Выполнено | 6 error-тестов: missing `;`, mismatched `()`, bad assignment target, chained `==`, chained `<`, unexpected token |
| **TEST-5** | Выполнено | Интеграция lexer+parser. Round-trip: `parse → JSON → decode → compare` в каждом тесте |

### Stretch Goal — Error Recovery (ERR-1 — ERR-4)

| Требование | Статус | Как реализовано |
| --- | --- | --- |
| **ERR-1** | Выполнено | Panic mode: `_synchronize()` пропускает до `;` или `}`. Phrase-level: `_consume()` с suggestion |
| **ERR-2** | Выполнено | Точки синхронизации: `;`, `}`, начало declaration/statement keywords |
| **ERR-3** | Выполнено | `ErrorMetrics`: reported_count, recovered_count, cascade_prevented_count |
| **ERR-4** | Выполнено | Множественные ошибки за один запуск. Suggestions (напр. "Did you forget a semicolon?"). `--max-errors N` |

---

## Команды для проверки

```bash
# Лексер
python -m cli lex --input examples/hello.src --output tokens.txt

# Парсер (текст)
python -m cli parse --input examples/hello.src --ast-format text

# Парсер (DOT для визуализации)
python -m cli parse --input examples/hello.src --ast-format dot --output ast.dot

# Парсер (JSON)
python -m cli parse --input examples/hello.src --ast-format json

# Парсер с verbose (ошибки + метрики)
python -m cli parse --input examples/hello.src --ast-format text --verbose

# Все тесты
python -m tests.test_runner

# Только парсер
python -m tests.test_runner --only parser

# Только семантика
python -m tests.test_runner --only semantic

# Полный отчёт семантики
compiler check --input examples/hello.src --output semantic_report.txt
compiler check --input examples/hello.src --show-types --report --verbose

# Дамп таблицы символов
compiler symbols --input examples/hello.src
compiler symbols --input examples/hello.src --format json --output symbols.json
```

## Итог

Выполнены требования **Sprint 1** (лексер, тесты, препроцессор), **Sprint 2** (грамматика, парсер, AST, визуализация, тесты, error recovery) и **Sprint 3** (семантика: таблица символов, типы, области видимости, проверки, decorated AST и отчёты).
